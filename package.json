{
  "name": "graphql-crash-course-netninja",
  "type": "module",
  "version": "1.0.0",
  "description": "This contains the content created during GraphQL crash course by Net Ninja.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "start": "npm run compile && node ./dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.9.3",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.6.5",
    "typescript": "^5.2.2"
  }
}
